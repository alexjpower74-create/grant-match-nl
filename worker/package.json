{
  "name": "grant-match-nl-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 7402",
    "migrate:local": "wrangler d1 migrations apply grant-match-nl --local",
    "test": "node tests/run.mjs"
  }
}
